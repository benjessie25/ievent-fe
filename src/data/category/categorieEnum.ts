export enum CategorieField {
  id = '_id',
  name = 'name',
  cover = 'cover',
  path = 'path',
  blurDataURL = 'blurDataURL',
  file = 'file',
  slug = 'slug',
  status = 'status',
  createdAt = 'createdAt',
}

export enum StatusCategorie {
  active = "Active",
  desactive = "Desactive"
}
